# Strategy Comparison

本文件现在只保留 legacy comparison 说明，不再提供 active truth。

## 当前规则
- 旧 universe 上的策略比较结果全部降级为历史对照
- 新 canonical universe 上只有 `baseline_limit_up` 的 baseline rebuild 还在进行
- 在 baseline rebuild 完成前，不允许据此宣布 `risk_constrained_limit_up` 或 `tighter_entry_limit_up` 仍是当前主线

## 历史结果使用边界
- 只能回答“旧样本上曾经发生过什么”
- 不能回答“当前真正应该研究什么”
