# 假设队列

1. [进行中] 先把 `cn_a_mainboard_all_v1` 的 bars 补齐到可重建 baseline 的最低要求，再判断 `baseline_limit_up` 在新 universe 上是否仍具备研究价值。
2. [待处理] 若新 baseline 建立后仍有可比性，再重新评估 `risk_constrained_limit_up` 是否值得回到 active track。
3. [待处理] 若新 baseline 建立后仍有可比性，再重新评估 `tighter_entry_limit_up` 是否值得回到 active track。
